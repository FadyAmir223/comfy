{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "tsc && concurrently \"tsc -w\" \"cross-env NODE_ENV=development nodemon dist/server.js\"",
    "build": "cp .env .env.production cert.pem key.pem package.json package-lock.json ../build && tsup && cross-env NODE_ENV=production node ../build/server/server.cjs",
    "cluster": "pm2 start dist/server.js -i max",
    "test": "tsc && jest",
    "test-watch": "concurrently \"tsc -w\" \"jest --watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^6.1.5",
    "mongoose": "^7.0.4",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.32.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/morgan": "^1.9.4",
    "@types/multer": "^1.4.7",
    "@types/node": "^18.16.0",
    "concurrently": "^8.0.1",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "tsup": "^6.7.0",
    "typescript": "^5.0.4"
  }
}
